<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.lynn.simplerecyclerview">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".colorselector.ChooseColorActivity"/>
        <activity android:name=".photocrop.ChoosePhotoActivity"/>
        <activity android:name=".photocrop.CropImageActivity"/>
        <activity android:name=".loading.LoadingActivity"/>
        <activity android:name=".drag.ViewDragHelperActivity"/>
        <activity android:name=".drag.DragActivity"/>
        <activity android:name=".theme.ThemeTestActivity"/>

        <service android:name=".serviceExample.TestService"/>
        <receiver android:name=".serviceExample.TestBroadCast"/>
        <provider
            android:name=".provider.FileProviderAndroidN"
            android:authorities="com.lynn.simplerecyclerview"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path"/>
        </provider>
    </application>

</manifest>